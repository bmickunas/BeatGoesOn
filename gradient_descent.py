

class GradientDescent(object):
        # <x, y> - training pair where x and y are song vectors
        # Compute Loss function with parameters x*w and y, where w is the vector of weights
        # Compute the adjustment to the weights, delta_w = alpha*(dL/dw), learning rate multiplied by the Gradient vector
        # Update the weights vector, w_2 = w_1 + delta_w



if __name__ == "__main__":